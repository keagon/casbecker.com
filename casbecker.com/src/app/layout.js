import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const metadata = {
  title: "Cas Becker - Developer & Social Entrepreneur",
  description: "Personal website of Cas Becker - Programmer and Social Entrepreneur passionate about Mendix, React-Native and societal issues.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
